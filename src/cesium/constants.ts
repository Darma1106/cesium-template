import * as Cesium from 'cesium'

export const Colors = Object.freeze({
  DEFAULT_LABEL_COLOR: Cesium.Color.CYAN.toBytes(),
  DEFAULT_LINE_COLOR: Cesium.Color.CYAN.toBytes(),
  DEFAULT_FLOW_LINE_COLOR: Cesium.Color.BLUEVIOLET.toBytes(),
  HIGHLIGHT_COLOR: Cesium.Color.YELLOW.toBytes()
})
